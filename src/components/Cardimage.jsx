import styled from "styled-components"

const CardImage = styled.img`
  width: 90%;
  aspect-ratio: 4 / 3;
  max-height: 200px;
  object-fit: cover;
  border-radius: 6px;
  margin: 0 auto;
`

export default CardImage
