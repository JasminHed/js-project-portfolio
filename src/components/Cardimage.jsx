import styled from "styled-components"

const CardImage = styled.img`
  width: 100%;
  height: auto;
  aspect-ratio: 4 / 3;
  max-height: 220px;
  object-fit: cover;
  margin-bottom: 10px;
`

export default CardImage
